const initalState = {
};

const recipesReducer = (state = initalState, action) => {
  var newState;
  switch (action.type) {
    default:
      newState = state;
  }
  return newState;
};

export default recipesReducer;
